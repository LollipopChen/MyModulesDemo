<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mymodulesdemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".base.AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning,UnusedAttribute"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.main.me.about.AboutActivity" />
        <activity android:name=".ui.web.WebViewActivity" />
        <activity
            android:name=".ui.search.SearchActivity"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity android:name=".ui.search.SearchResultActivity" />
        <activity android:name=".ui.main.navigation.activity.SingleNavigationActivity" />
        <activity android:name=".ui.main.me.video.VideoActivity" />
        <activity android:name=".ui.main.me.message.MessageActivity"
            android:launchMode="singleTask" />
        <activity android:name=".ui.main.me.info.UserInfoActivity"
            android:launchMode="singleTask" />
        <activity android:name=".ui.main.me.setting.SettingActivity" />
        <activity android:name=".ui.main.me.collection.CollectionActivity"
            android:launchMode="singleTask" />
        <activity android:name=".ui.main.me.widget.WidgetActivity" />

    </application>

</manifest>