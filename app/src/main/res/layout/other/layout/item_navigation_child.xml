<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.example.mymodulesdemo.ui.main.navigation.viewmodel.SingleNavigationChildItemViewModel"/>
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/dimen_10_dp"
        app:onClickCommand="@{viewModel.onItemClick}">

        <TextView
            android:id="@+id/tv_author"
            android:layout_width="@dimen/dimen_0_dp"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintRight_toLeftOf="@+id/tv_date"
            android:textSize="@dimen/text_size_12_sp"
            android:textColor="@color/gray"
            tools:text="哈哈"
            android:text="@{viewModel.observableField.author}"/>

        <TextView
            android:id="@+id/tv_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:textSize="@dimen/text_size_12_sp"
            android:textColor="@color/gray"
            tools:text="2019-02-21"
            android:text="@{viewModel.observableField.niceDate}"/>

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="@dimen/dimen_0_dp"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tv_author"
            android:textSize="@dimen/text_size_16_sp"
            android:textColor="@color/text_color"
            tools:text="2019-02-21"
            android:text="@{viewModel.observableField.title}"/>

        <TextView
            android:id="@+id/tv_flag"
            android:layout_width="@dimen/dimen_0_dp"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tv_title"
            app:layout_constraintRight_toRightOf="parent"
            android:textSize="@dimen/text_size_12_sp"
            android:textColor="@color/gray"
            tools:text="哈哈"
            android:text="@{viewModel.observableField.superChapterName + @string/cut_line + viewModel.observableField.chapterName}"/>

    </android.support.constraint.ConstraintLayout>

</layout>