<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:binding="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.example.mymodulesdemo.ui.web.WebViewModel"/>
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <include
            android:id="@+id/layout_bar"
            layout="@layout/layout_back_title_bar"
            binding:toolbarViewModel="@{viewModel}"/>

        <com.example.libbase.widget.ProgressWebView
            android:layout_width="@dimen/dimen_0_dp"
            android:layout_height="@dimen/dimen_0_dp"
            binding:layout_constraintLeft_toLeftOf="parent"
            binding:layout_constraintRight_toRightOf="parent"
            binding:layout_constraintTop_toBottomOf="@+id/layout_bar"
            binding:layout_constraintBottom_toBottomOf="parent"
            binding:link="@{viewModel.urlObservable}"/>

    </android.support.constraint.ConstraintLayout>

</layout>